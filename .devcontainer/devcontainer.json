{
  "name": "ReadYou",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "onCreateCommand": "sudo apk add --no-cache py3-pip || sudo apt-get update && sudo apt-get install -y python3-pip; python3 -m pip install --user --break-system-packages --quiet pipx && python3 -m pipx ensurepath && python3 -m pipx install mcp-server-fetch duckduckgo-mcp-server",
  "customizations": {
    "vscode": {
      "extensions": [
        "github.vscode-pull-request-github"
      ]
    }
  }
}
